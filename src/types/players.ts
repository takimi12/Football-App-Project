export interface PlayerMongo {
	_id: string
	firstName: string
	lastName: string
	teamId: string | null
}
export interface PlayerLocal {
	id: string
	firstName: string
	lastName: string
	teamId: string | null
}
